# PyTorch with CUDA support
--extra-index-url https://download.pytorch.org/whl/cu118
torch
torchvision
torchaudio

# ML & DL
huggingface_hub
torchtext
torchinfo
diffusers
wandb # ==0.13.10
accelerate
ftfy
safetensors
openai>=1.56.2
sentence-transformers
peft
bitsandbytes

# Data manipulation and analysis
einops
eindex-callum
pandas
numpy-stl
scikit-learn
hdbscan
umap-learn

# Utilities
requests
pytest
tqdm
ipykernel
notebook
ipywidgets==7.7.1
jupyterlab-widgets==1.1.1
jupyter
pre-commit
gdown
portalocker>=2.0.0
typeguard
jaxtyping
tiktoken
frozendict
tabulate
opencv-python
nbformat
docformatter
pyyaml

# Visualization
matplotlib
plotly
streamlit
streamlit-antd-components==0.2.5
streamlit-image-select
streamlit-on-Hover-tabs
nnsight>=0.3.5
imageio-ffmpeg
mediapy

# Chapter 1 (interp)
transformer_lens>=2.16.1,<3.0.0
transformers>=4.51,<5
sae-lens>=6.37.4,<7.0.0
sae-vis==0.3.7
git+https://github.com/callummcdougall/CircuitsVis.git#subdirectory=python
git+https://github.com/neelnanda-io/neel-plotly

# Chapter 2 (RL)
pygame
gymnasium[atari, accept-rom-license, other, mujoco-py]==0.29.0
mujoco
# Rendering MuJoCo videos additionally needs a system OpenGL backend, which pip cannot provide:
# `sudo apt-get install libosmesa6`, or without sudo `conda create -n oslibs -c conda-forge
# "mesalib=24.3.4"` (mesalib >= 25 dropped OSMesa). Backend selection + the conda-lib preload live
# in part3_ppo/mujoco_rendering.py; see the [2.3] "Installation & Rendering" section. The Python
# side (PyOpenGL, glfw) comes in as mujoco dependencies, and mp4 encoding uses imageio-ffmpeg.
# GPU physics for the MuJoCo section (Brax `positional` backend + our torch PPO). Pinned because
# brax only declares `jax>=0.4.6` (no upper bound), so an unpinned install grabs a newer jax whose
# removed `device_put_replicated` breaks brax. Capped at 0.7.1 (not 0.10.1) because jax >= 0.7.2
# requires numpy >= 2.0, which conflicts with circuitsvis's numpy < 2.0 cap above; 0.7.1 is the
# newest jax that still allows numpy < 2.0 and still exports `device_put_replicated` for brax.
# NOTE: this only resolves on Python <= 3.12 — on 3.13+, jax's ml-dtypes>=0.5.0 dep pulls
# numpy >= 2.1.0 and the circuitsvis conflict returns. The arena-env is Python 3.11.
jax[cuda12]==0.7.1
brax==0.14.2
# Vectorised Atari emulators (C++) for the Atari bonus section's AtariEnvs.
envpool==1.2.5

# Chapter 3 (evals)
inspect_ai
tabulate
wikipedia
python-dotenv
anthropic>=0.42.0
instructor

hf_transfer
datasets